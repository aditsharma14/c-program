// wap to find the largest of two numbers.
#include <stdio.h>
int main()
{
    int a, b;
    scanf("%d", &a);
    scanf("%d", &b);
    if (a > b)
    {
        printf("first number is greater than the second");
    }
    else
    {
        printf("second one is greater than the first one");
    }
}
