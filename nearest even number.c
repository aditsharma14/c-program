#include <stdio.h>
int main()
{
    int a;
    printf("enter the number:");
    scanf("%d", &a);
    if (a % 2 == 0)
    {
        printf("the nearest even no is:%d", a);
    }
    else
    {
        printf("the nearest even no is:%d", a);
    }
    return 0;
}